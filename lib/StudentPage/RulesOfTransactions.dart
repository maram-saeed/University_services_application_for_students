import 'package:flutter/material.dart';

class RulesOfTransactions extends StatelessWidget {
  const RulesOfTransactions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
